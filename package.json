{
  "name": "decoder-ring",
  "version": "0.0.1",
  "description": "Decode JSON into application types",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": "Scott Rabin <scottrabin@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "lint": "tslint --project ./tsconfig.json",
    "test": "mocha",
    "test:watch": "mocha --reporter list --growl",
    "watch": "npm-watch"
  },
  "watch": {
    "test:watch": {
      "patterns": [
        "lib",
        "test"
      ],
      "extensions": "ts"
    }
  },
  "dependencies": {
    "typescript": "^2.1.5"
  },
  "devDependencies": {
    "@types/chai": "^3.4.34",
    "@types/faker": "^4.1.0",
    "@types/mocha": "^2.2.35",
    "chai": "^3.5.0",
    "faker": "^4.1.0",
    "mocha": "^3.2.0",
    "npm-watch": "^0.1.7",
    "ts-node": "^2.1.0",
    "tslint": "^4.5.1"
  }
}
